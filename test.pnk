fun main() {
  /*
   * Block comment
  */
  var my_base = @base;
  var my_value = 42;
  var len = 1;
  @printf(my_base, len, @base, 1);
  st my_value, my_base;
  var my_value2 = 0;
  my_value2 = lds {3, {2, 1}} my_base;

  // Line comment
  foo();
  return 0;
}

fun foo() {
  return 0;
}
